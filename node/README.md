# Encryption
- Find asymetric encryption
- use the pubkey as the user id
- have a way users to introduce themselves.  It is a message containing their pubkey and their alias


# notifee
    /// TODO: Optimization for notifee:
    /// 1) Have notifee send/receive the UserId which sent the data
    /// 2) have different notifee channels for each type of data
    /// so we would have a dm_notifee which would receive the 
    This might need some rework.